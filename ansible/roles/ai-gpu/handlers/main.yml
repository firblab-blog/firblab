---
# =============================================================================
# AI GPU Guest VM — Handlers
# =============================================================================

- name: Restart ai-gpu
  ansible.builtin.command:
    cmd: docker compose up -d --force-recreate
    chdir: "{{ ai_gpu_install_dir }}"
